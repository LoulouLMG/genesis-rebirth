Ici tous les fichiers qui vont être lancés une seule fois pour l'initialisation de l'application :  par exemple le fichier.sql qui va générer les tables, les fichiers php qui vont initialisaer l'univers, etc ...